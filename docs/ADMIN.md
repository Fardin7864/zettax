# Admin

The operations console is a separate Next.js application. Phase 1 provides an original responsive shell and clear environment/compliance status. Subsequent modules require admin authentication, TOTP, session controls, granular RBAC, object-level checks, immutable audit events, and maker-checker rules. No screen may expose a direct editable balance; adjustments are ledger workflows with reason and reference.
